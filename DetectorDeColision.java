class DetectorDeColision {
    // Métodos estáticos para detectar colisiones:
    // - verificarColision(NaveEspacial Nave, Asteroide asteroide): boolean
    // - intersectarPoligono(Poligono p1, Poligono p2): boolean
    // - puntoPoligono(Punto p, Poligono poli): boolean
}